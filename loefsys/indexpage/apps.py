"""Module containing the configuration for the indexpage app."""

from django.apps import AppConfig


class IndexpageConfig(AppConfig):
    """The app configuration for the indexpage."""

    name = "loefsys.indexpage"
